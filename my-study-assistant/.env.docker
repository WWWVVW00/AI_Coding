# Docker 环境配置文件
# 这个文件包含 Docker 容器的环境变量配置

# 数据库配置
DB_HOST=database
DB_PORT=3306
DB_USER=root
DB_PASSWORD=admin123456
DB_NAME=study_assistant

# JWT 配置
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production_docker_env
JWT_EXPIRES_IN=7d

# 服务器配置
PORT=3001
NODE_ENV=production

# 文件存储配置
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=50MB

# 日志配置
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# 前端配置
VITE_API_URL=http://localhost:3001

# Redis 配置（如果需要）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# 邮件服务配置（可选）
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=